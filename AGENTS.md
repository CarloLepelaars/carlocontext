# carlocontext

This repo *is* the plugin. Skills live in `skills/<name>/SKILL.md`.

Do not put always-on style rules in this file — they only apply inside this repo. Put them in a skill so they apply after install.

No Python package, no `src/`, no tests unless a skill needs a script.
