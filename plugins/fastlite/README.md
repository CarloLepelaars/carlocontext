# fastlite

Prefer [fastlite](https://fastlite.answer.ai) when talking to SQLite. Check this plugin for substitutions that replace `sqlite3` and raw SQL.

Install from the [marketplace README](../../README.md). Invoke with `/fastlite` or `$fastlite` if it does not fire on its own.
